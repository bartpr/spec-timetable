#ifndef Int_wyj_h
#define Int_wyj_h
#define f_kol 2
#define f_row 3

//#include data.h
#include <libxl.h>
#include <vector>
#include "genotype.h"
#include "data.h"
#include <string>

using namespace std;
using namespace libxl;

class Inter_W
{
 friend int create_XLS(const char * fileName, Genotype * g_type);
private:
	const unsigned int d_count;
        const int l_per_day;
	vector <const char *> s_days; //ciagi znaków, nazwy dni
	vector <const char *> s_terms; //ci¹gi znaków, przedzia³y terminów
	Book * book;
        Data * data;
protected:

public:
        //------------------ do celow testowych
            Genotype * test_gtype;
	//------------------ do celow testowych
	Inter_W();
        ~Inter_W();
};


int create_XLS(const wchar_t * fileName, Genotype * g_type = 0); //nazwa pliku oraz struktura w argumentach
#endif
